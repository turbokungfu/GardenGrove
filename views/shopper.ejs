<%- include('partials/header') -%>


      </script>
        <div class="container my-3">
          <h1 class="display-4 text-center">
            <i class="fas fa-map-marked mr-4"></i>Gardens Near Me
          </h1></div>
          <div class="container float-start col-6">
            <div
              id="map"
              style="width: 500px; height: 500px;border-radius: 5px;"
            ></div>
          </div>
        </div>
  

        <div class="container col-12 col-sm-12 col-md-12 col-lg-6 float-end ">
          <ul class="row list-unstyled">
       <li class="box">
          <% for(var i=0; i<posts.length; i++){ %>
            <a href="/login">
              
              <img class="img-fluid pic" src="<%= posts[i].image%>"> 
            </a>
  
  
          <div class="side">
          <span style="font-weight: bold"> <%= posts[i].title  %></span>
         <br>
          <%= posts[i].location.formattedAddress%>
          <br>
          <span style="padding-bottom: 5px"><%= posts[i].caption.length <= 150? posts[i].caption: posts[i].caption.slice(0, 150)+'...' %></span>
        </div>
              <%}%>
  
            </li>
          </ul>
      </div>

  
  <div class="col-4 float-start">
    <p><strong>User Name</strong>: <%= user.userName %></p>
    <p><strong>Email</strong>: <%= user.email %></p>
    <a href="/logout" class="col-3 btn btn-primary">Logout</a>
</div>
  <script src="https://api.mapbox.com/mapbox-gl-js/v1.4.1/mapbox-gl.js"></script>
  <script src="js/map.js"></script>
  <script src="js/add.js"></script>
</body>
<%- include('partials/footer') -%>